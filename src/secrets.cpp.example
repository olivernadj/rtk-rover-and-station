#include "secrets.h"

// ── WiFi ────────────────────────────────────────────────────────────────────
const WifiCredential WIFI_CREDENTIALS[] = {
    {"your_ssid_1", "your_password_1"},
    {"your_ssid_2", "your_password_2"},
};
const int WIFI_CREDENTIAL_COUNT = sizeof(WIFI_CREDENTIALS) / sizeof(WIFI_CREDENTIALS[0]);

// ── MQTT ─────────────────────────────────────────────────────────────────────
const char* MQTT_HOST      = "mqtt.example.com";
const int   MQTT_PORT      = 1883;

// ── NTRIP broadcaster (stationary only) ─────────────────────────────────────
#ifdef MODE_STATIONARY
const NtripCasterConfig NTRIP_CASTERS[] = {
    {
        "rtk2go.com",
        2101,
        "YOUR_MOUNTPOINT",
        "your_rtk2go_user",
        "your_rtk2go_password"
    },
    {
        "caster.emlid.com",
        2101,
        "YOUR_MOUNTPOINT",
        "your_emlid_user",
        "your_emlid_password"
    },
};
const int NTRIP_CASTER_COUNT = sizeof(NTRIP_CASTERS) / sizeof(NTRIP_CASTERS[0]);
#endif

// ── NTRIP client (rover only) ────────────────────────────────────────────────
#ifdef MODE_ROVER
const char* NTRIP_HOST       = "rtk2go.com";
const int   NTRIP_PORT       = 2101;
const char* NTRIP_MOUNTPOINT = "YOUR_MOUNTPOINT";
const char* NTRIP_USER       = "your_user";
const char* NTRIP_PASSWORD   = "your_password";
#endif

// ── OTA firmware update (when OTA_ENABLED) ──────────────────────────────────
#ifdef OTA_ENABLED
const char* OTA_USER     = "otauser";
const char* OTA_PASSWORD = "your_ota_password";
#endif
